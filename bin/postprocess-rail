#!/bin/bash

DB=empirelogistics

echo "Postprocessing rail data - select into new table"
psql -U el -q -d $DB < etc/schema/rail_line.sql
psql -U el -q -d $DB < etc/schema/rail_node.sql
psql -U el -q -d $DB < etc/schema/rail_interline.sql
