{
    "cache": {
        "name": "Disk",
        "path": "/var/local/tilestache",
        "dirs": "portable"
    },
    "logging": "warning",
    "layers": {
        "lines": {
            "allowed origin": "*",
            "provider": {
                "class": "TileStache.Goodies.VecTiles:Provider",
                "kwargs": {
                    "clip": true,
                    "dbinfo": {
                        "host": "localhost",
                        "database": "empirelogistics",
                        "user": "el"
                    },
                    "queries": [
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 7",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 7",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 7",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 7",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 7",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 6",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 5",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 4",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 3",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 2",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 1",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 0",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 0",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 0",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 0",
                        "select geom as __geometry__, rtid as name, miles, densty, sb from raw_rail_line where densty::integer >= 0"
                    ]
        }}}
    }
}
